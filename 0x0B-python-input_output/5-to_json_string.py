#!/usr/bin/python3
"""Function that allows convert to obj json from string"""
import json


def to_json_string(my_obj):
    """Function: convert to obj json from str"""
    return (json.dumps(my_obj))
